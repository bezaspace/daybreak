/**
 * Control plane placeholder.
 *
 * Phase 1 will add a local Node.js/TypeScript server here, structured so it
 * can be ported to Cloudflare Workers with minimal changes.
 */
export function createControlPlane(): { status: string } {
  return { status: "not implemented in Phase 0" };
}
