{
  "name": "@daybreak/agent-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Daybreak agent runner: Pi SDK loop with safety, metrics, and checkpointing",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "spike": "tsx src/spike.ts",
    "task": "tsx src/run-task.ts",
    "sandbox": "tsx src/sandbox.ts",
    "lint": "tsc --noEmit && echo 'agent-runner lint OK'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --noEmit",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@daybreak/shared": "workspace:*",
    "@daytona/sdk": "^0.203.0",
    "@earendil-works/pi-agent-core": "0.83.0",
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "picocolors": "1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
